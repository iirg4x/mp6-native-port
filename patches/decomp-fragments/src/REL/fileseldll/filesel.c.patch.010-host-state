--- a/src/REL/fileseldll/filesel.c
+++ b/src/REL/fileseldll/filesel.c
@@ -1307,0 +1308,5 @@
+    if (i >= 4) {
+        HuPrcEnd();
+        return;
+    }
+    prevState = p->visualState;
@@ -1516 +1521 @@
-    proc->property = (void *)arg1;
+    proc->property = (void *)(intptr_t)arg1;
@@ -1528 +1533 @@
-    bank = (s32)proc->property;
+    bank = (s32)(intptr_t)proc->property;
@@ -2065 +2070 @@
-                proc2->property = (void *)a;
+                proc2->property = (void *)(intptr_t)a;
@@ -2129 +2134 @@
-    idx = (s32)proc->property;
+    idx = (s32)(intptr_t)proc->property;
