--- a/src/board/scroll.c
+++ b/src/board/scroll.c
@@ -343,0 +344,13 @@
+
+/* MP6 native port -- read-only accessor for this file's own
+ * `scrollColModel` static.  platform/os/board_starscroll.c transcribes
+ * retail's CheckScrollCol (.text:0x80171B4C) to raycast the board's
+ * scroll-collision mesh for the star-reveal camera pan; that routine is a
+ * scroll.c file-local on hardware and has direct access to this static,
+ * which a separate translation unit cannot get any other way.  Adds no
+ * behavior and changes no existing line; delete it together with
+ * board_starscroll.c once scroll.c grows the real mbev_StarScroll. */
+HU3D_MODELID mp6ScrollColModelGet(void)
+{
+    return scrollColModel;
+}
