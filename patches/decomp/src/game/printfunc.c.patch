--- a/src/game/printfunc.c
+++ b/src/game/printfunc.c
@@ -4,0 +5 @@
+#include "mp6_widescreen.h" /* widescreen support */
@@ -297 +298,6 @@
-                            if(x > HU_FB_WIDTH) {
+                            /* The reference widescreen hack widens this
+                             * same check to a fixed value; generalized to
+                             * the live render width here. 640 (byte-
+                             * identical) when disabled. */
+                            if(x > mp6_widescreen_render_width()) {
