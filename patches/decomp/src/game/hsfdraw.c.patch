--- a/src/game/hsfdraw.c
+++ b/src/game/hsfdraw.c
@@ -14,0 +15,3 @@
+#include "mp6_shadow_quality.h" /* shadow quality: effective map size */
+#include "mp6_fi_model.h" /* deferred-draw matrix identity (Unlocked FPS) */
+#include <stdint.h>
@@ -25,0 +27,54 @@
+
+/* Object-level draw census, the per-OBJECT companion to hsfman.c's
+ * per-MODEL one (that census explains what the Hu3DExec loop decided;
+ * this one, behind its own MP6_DRAW_CENSUS_OBJ=<tick>, explains what
+ * happened INSIDE each Hu3DDraw tree walk: which objects were visited,
+ * which were skipped by LOCK/DISPOFF-flags/zero-scale, which deferred to
+ * the z-sorted XLU list, and which reached ObjDraw). Same two-tick window
+ * semantics as the hsfman census; zero output and one cached getenv when
+ * unset. Diagnostic only: prints state, never changes behavior. */
+#include <stdio.h>
+#include <stdlib.h>
+extern long mp6_tick_count;
+static long mp6ObjCensusTick = -2; /* -2 = env not parsed yet, -1 = disabled */
+static int mp6ObjCensusActive(void)
+{
+    if (mp6ObjCensusTick == -2) {
+        const char *e = getenv("MP6_DRAW_CENSUS_OBJ");
+        mp6ObjCensusTick = (e && atol(e) > 0) ? atol(e) : -1;
+    }
+    return mp6ObjCensusTick > 0 && mp6_tick_count >= mp6ObjCensusTick &&
+           mp6_tick_count < mp6ObjCensusTick + 2;
+}
+
+/* Display-list / vertex ground-truth dump, used to track down a
+ * mode-select/file-select "vertices all over the place" report with
+ * actual bytes instead of more theories: for the one object named by
+ * MP6_DL_DUMP=<name> this dumps, at DL-BUILD time (MDObjMesh, i.e.
+ * model-create), to build/dldump_<name>.txt:
+ *   - the parsed face records (type/mat/index tuples) the DL was built from,
+ *   - the vertex/st array contents summary (count, first entries, min/max
+ *     magnitude, out-of-range-index scan over every face tuple),
+ *   - the referenced materials' vtxMode/attrNum/flags (the vertex-descriptor
+ *     selectors), and
+ *   - the raw built DL bytes (first 1 KB hex) + per-batch dlOfs table.
+ * Decoding those bytes against the descriptor the draw path will set is a
+ * mechanical, offline check of stream-vs-descriptor alignment. One getenv,
+ * zero output when unset. Diagnostic only: never changes behavior. */
+#include <string.h>
+static int mp6DlDumpParsed = 0;       /* 0 = env not parsed yet */
+static const char *mp6DlDumpName = NULL;
+static const char *mp6DlDumpEnv(void)
+{
+    if (!mp6DlDumpParsed) {
+        mp6DlDumpName = getenv("MP6_DL_DUMP");
+        mp6DlDumpParsed = 1;
+    }
+    return mp6DlDumpName;
+}
+static int mp6DlDumpWant(const char *objName)
+{
+    const char *want = mp6DlDumpEnv();
+    return want && objName && strcmp(want, objName) == 0;
+}
@@ -52,2 +109,2 @@
-static s32 MakeCalcNBT(HSF_OBJECT *objPtr, HSF_FACE *face, s16 endVtx, s16 startVtx);
-static s32 MakeNBT(HSF_OBJECT *objPtr, HSF_FACE *face, s16 endVtx, s16 startVtx);
+static void MakeCalcNBT(HSF_OBJECT *objPtr, HSF_FACE *face, s16 endVtx, s16 startVtx);
+static void MakeNBT(HSF_OBJECT *objPtr, HSF_FACE *face, s16 endVtx, s16 startVtx);
@@ -138 +140,6 @@
-    DrawObjIdx = 0;
+    DrawObjIdx = 0;
+    /* Unlocked FPS (shim/include/mp6_fi_model.h): a new deferred batch starts
+     * here, so drop the previous one's reserved identities. Diagnostic-free
+     * bookkeeping; nothing about the draw itself changes. */
+    mp6_fi_capture_defer_reset();
@@ -149,0 +205,6 @@
+    if (mp6ObjCensusActive()) { /* object census -- see mp6ObjCensusActive above */
+        /* modelP->hsf and modelP->hookFunc are the SAME union member
+         * (include/game/hu3d.h): for a hook-func model -- every particle
+         * system (mbParticleCreate) and every raw Hu3DHookFuncCreate --
+         * that word holds a FUNCTION pointer and there is no HSF_DATA at
+         * all. Reading hsf->root before the HU3D_ATTR_HOOKFUNC branch below
+         * therefore chased a code address as if it were a scene graph, and
+         * killed the census on the first hook-func model of any board tick
+         * (access violation reading 0xFFFFFFFFFFFFFFFF: hsf->root came back
+         * all-ones out of the code page, and ->type sits at offset 0). The
+         * census could consequently never complete a single board frame,
+         * which is exactly the frame content a flicker investigation needs
+         * enumerated. Print the HSF fields only when there IS an HSF. */
+        if (modelP->attr & HU3D_ATTR_HOOKFUNC) {
+            printf("[CENSUS-OBJ] Hu3DDraw model=%d attr=%08x HOOKFUNC hook=%p motId=%d (no HSF: union holds the hook)\n",
+                   (int)(modelP - Hu3DData), (unsigned)modelP->attr, (void *)modelP->hookFunc, modelP->motId);
+        } else if (hsf == NULL) {
+            printf("[CENSUS-OBJ] Hu3DDraw model=%d attr=%08x hsf=NULL motId=%d\n",
+                   (int)(modelP - Hu3DData), (unsigned)modelP->attr, modelP->motId);
+        } else {
+            printf("[CENSUS-OBJ] Hu3DDraw model=%d attr=%08x hsf=%p root=%p rootType=%d objectNum=%d motId=%d\n",
+                   (int)(modelP - Hu3DData), (unsigned)modelP->attr, (void *)hsf, (void *)hsf->root,
+                   hsf->root ? hsf->root->type : -1, hsf->objectNum, modelP->motId);
+        }
+        fflush(stdout);
+    }
@@ -159 +221,6 @@
-        DrawObjIdx++;
+        /* Unlocked FPS: this hook-func (particle) model is being DEFERRED to
+         * Hu3DDrawPost, which emits its matrices after the model bracket has
+         * closed. Reserve its identity here, while the bracket is still open. */
+        mp6_fi_capture_defer_push(DrawObjIdx);
+        DrawObjIdx++;
@@ -189,0 +251,7 @@
+    if (mp6ObjCensusActive()) { /* object census (rot/pos: the sky-wheel angle) */
+        printf("[CENSUS-OBJ]  objCall type=%d name=%s childNum=%d baseRot=(%.1f,%.1f,%.1f) basePos=(%.1f,%.1f,%.1f) curRot=(%.1f,%.1f,%.1f)\n",
+               objPtr->type, objPtr->name ? objPtr->name : "(null)", objPtr->mesh.childNum,
+               objPtr->mesh.base.rot.x, objPtr->mesh.base.rot.y, objPtr->mesh.base.rot.z,
+               objPtr->mesh.base.pos.x, objPtr->mesh.base.pos.y, objPtr->mesh.base.pos.z,
+               objPtr->mesh.curr.rot.x, objPtr->mesh.curr.rot.y, objPtr->mesh.curr.rot.z);
+    }
@@ -256,0 +325,3 @@
+    if (mp6ObjCensusActive() && (constData->attr & HU3D_CONST_LOCK)) { /* object census */
+        printf("[CENSUS-OBJ]  mesh %s -> skip:LOCK\n", objPtr->name ? objPtr->name : "(null)");
+    }
@@ -338,0 +410,19 @@
+            }
+            if (mp6ObjCensusActive()) { /* object census: predict the branch below, in its order */
+                const char *mp6Br;
+                if (!dispF) {
+                    mp6Br = "skip:dispF(cull/DISPOFF-flag)";
+                } else if (transformP->scale.x == 0.0f && transformP->scale.y == 0.0f && transformP->scale.z == 0.0f) {
+                    mp6Br = "skip:zero-scale";
+                } else if (!(constData->attr & HU3D_CONST_REFLECTMODEL) &&
+                           ((constData->attr & (HU3D_CONST_NEAR|HU3D_CONST_ALTBLEND|HU3D_CONST_XLU)) && shadowModelDrawF == FALSE)) {
+                    mp6Br = "deferred-xlu";
+                } else if (modelP->attr & HU3D_ATTR_ZCMP_OFF) {
+                    mp6Br = "deferred-zoff";
+                } else {
+                    mp6Br = "ObjDraw";
+                }
+                printf("[CENSUS-OBJ]  mesh %s -> %s constAttr=%08x objFlags=%08x curScale=(%.2f,%.2f,%.2f)\n",
+                       objPtr->name ? objPtr->name : "(null)", mp6Br, (unsigned)constData->attr,
+                       (unsigned)objPtr->flags, transformP->scale.x, transformP->scale.y, transformP->scale.z);
+                fflush(stdout);
@@ -354 +445,3 @@
-                    DrawObjIdx++;
+                    /* Unlocked FPS: deferred translucent/NEAR object -- reserve
+                     * its identity before Hu3DDrawPost emits it out of order. */
+                    mp6_fi_capture_defer_push(DrawObjIdx);
+                    DrawObjIdx++;
@@ -361 +453,3 @@
-                    DrawObjIdx++;
+                    /* Unlocked FPS: deferred z-compare-off object, same. */
+                    mp6_fi_capture_defer_push(DrawObjIdx);
+                    DrawObjIdx++;
@@ -684,2 +742,26 @@
-                    texAnimP = &Hu3DTexAnimData[animWorkP->animId];
-                    if((animWorkP->attr & HU3D_ATTRANIM_ATTR_ANIM2D) && !(texAnimP->attr & HU3D_ANIM_ATTR_NOUSE)) {
+                    /* native port: the original computed
+                     * `texAnimP = &Hu3DTexAnimData[animWorkP->animId];`
+                     * BEFORE testing the ANIM2D flag. animId is only ever
+                     * ASSIGNED by Hu3DAnimCreate/Hu3DAnimLink (hsfanim.c),
+                     * which also set ANIM2D; a work created by
+                     * SetObjAttrMotion (attribute POS/ROT/SCALE/BITMAP
+                     * motion tracks, hsfmotion.c:1178) initializes attr and
+                     * the 3D transform fields but LEAVES animId
+                     * UNINITIALIZED -- so for every such work the retail
+                     * code computed a wild address it then never
+                     * dereferenced (ANIM2D clear, branch not taken; benign
+                     * on console). The bounds-checked native build panics
+                     * on the address computation itself. Measured,
+                     * deterministic across two runs on the retail W01
+                     * Party route ~38s after w01.render (first board scene
+                     * with attribute-track motions): "index 22086 out of
+                     * bounds for type 'HU3D_TEXANIM[256]'" here, with the
+                     * work's attr a legitimate ANIM3D and animId = heap
+                     * garbage 0x5646 ('F','V' of a recycled "HSFV037"
+                     * image block; build/w01party/verify1+verify2).
+                     * Reorder so the index is computed only under the flag
+                     * that guarantees a live animId -- observably
+                     * identical to retail. */
+                    if((animWorkP->attr & HU3D_ATTRANIM_ATTR_ANIM2D)
+                     && !((texAnimP = &Hu3DTexAnimData[animWorkP->animId])->attr & HU3D_ANIM_ATTR_NOUSE)) {
@@ -1161 +1251 @@
-            GXSetTevOrder(tevStage, GX_TEXMAP_NULL, GX_TEXCOORD_NULL, GX_COLOR0A0);
+            GXSetTevOrder(tevStage, GX_TEXCOORD_NULL, GX_TEXMAP_NULL, GX_COLOR0A0);
@@ -1432 +1522 @@
-            GXSetTevOrder(tevStage, GX_TEXMAP_NULL, GX_TEXCOORD_NULL, GX_COLOR0A0);
+            GXSetTevOrder(tevStage, GX_TEXCOORD_NULL, GX_TEXMAP_NULL, GX_COLOR0A0);
@@ -1836 +1926,6 @@
-    GXInitTexObj(&texObj, Hu3DShadow->buf, Hu3DShadow->size, Hu3DShadow->size, GX_TF_I8, GX_CLAMP, GX_CLAMP, GX_FALSE);
+    /* The texObj's claimed dims track the scaled copy DESTINATION
+     * (mp6_shadow_effective_size()), matching what Hu3DShadowExec's
+     * GXSetTexCopyDst resolved into Hu3DShadow->buf. Identity at the
+     * native setting. */
+    s32 effSize = mp6_shadow_latched_size(Hu3DShadow->buf, Hu3DShadow->size); /* create-time size, not live config */
+    GXInitTexObj(&texObj, Hu3DShadow->buf, (u16)effSize, (u16)effSize, GX_TF_I8, GX_CLAMP, GX_CLAMP, GX_FALSE);
@@ -1995,0 +2091,7 @@
+    }
+    if (mp6ObjCensusActive()) { /* object census: name the texture each draw binds */
+        printf("[CENSUS-OBJ]    LoadTexture bmp=%s data=%p fmt=%d %dx%d texId=%d attr=%s tint=(%d,%d,%d,%d)\n",
+               bmpPtr->name ? bmpPtr->name : "(null)", (void *)bmpPtr->data, bmpPtr->dataFmt,
+               bmpPtr->sizeX, bmpPtr->sizeY, texId, attrP->name ? attrP->name : "(null)",
+               bmpPtr->tint.r, bmpPtr->tint.g, bmpPtr->tint.b, bmpPtr->tint.a);
+        fflush(stdout);
@@ -2124,0 +2227,5 @@
+            if (mp6ObjCensusActive()) { /* object census: whole subtree pruned here */
+                printf("[CENSUS-OBJ]  node %s -> skip:zero-scale (subtree pruned, childNum=%d)\n",
+                       objPtr->name ? objPtr->name : "(null)", objPtr->mesh.childNum);
+                fflush(stdout);
+            }
@@ -2174,0 +2282,5 @@
+            if (mp6ObjCensusActive()) { /* object census: whole subtree pruned here */
+                printf("[CENSUS-OBJ]  node %s -> skip:zero-scale (subtree pruned, childNum=%d)\n",
+                       objPtr->name ? objPtr->name : "(null)", objPtr->mesh.childNum);
+                fflush(stdout);
+            }
@@ -2386 +2498,6 @@
-            drawObj = &DrawObjData[DrawObjNum[drawObjNo]];
+            drawObj = &DrawObjData[DrawObjNum[drawObjNo]];
+            /* Unlocked FPS: re-install the identity reserved for this draw
+             * object at PUSH time. DrawObjNum[] is the depth-sorted emission
+             * order; DrawObjNum[drawObjNo] is the stable push slot, which is
+             * what the reservation was keyed by. */
+            mp6_fi_capture_defer_begin(DrawObjNum[drawObjNo]);
@@ -2387,0 +2500,8 @@
+                if (mp6ObjCensusActive()) { /* object census: the hook-func (particle) draws -- the one class the object walk above can never enter, because a hook-func model has no scene graph to walk */
+                    printf("[CENSUS-OBJ]   PostDraw #%d model=%d HOOKFUNC hook=%p z=%.1f mtxR0=(%.3f,%.3f,%.3f,%.1f) mtxR1=(%.3f,%.3f,%.3f,%.1f) mtxR2=(%.3f,%.3f,%.3f,%.1f)\n",
+                           drawObjNo, (int)(drawObj->model - Hu3DData), (void *)drawObj->model->hookFunc, drawObj->z,
+                           drawObj->matrix[0][0], drawObj->matrix[0][1], drawObj->matrix[0][2], drawObj->matrix[0][3],
+                           drawObj->matrix[1][0], drawObj->matrix[1][1], drawObj->matrix[1][2], drawObj->matrix[1][3],
+                           drawObj->matrix[2][0], drawObj->matrix[2][1], drawObj->matrix[2][2], drawObj->matrix[2][3]);
+                    fflush(stdout);
+                }
@@ -2399,0 +2512,6 @@
+                if (mp6ObjCensusActive()) { /* object census: deferred z-sorted draws */
+                    printf("[CENSUS-OBJ]   PostDraw #%d model=%d obj=%s z=%.1f\n",
+                           drawObjNo, (int)(drawObj->model - Hu3DData),
+                           drawObj->object->name ? drawObj->object->name : "(null)", drawObj->z);
+                    fflush(stdout);
+                }
@@ -2488 +2606,3 @@
-            }
+            }
+            /* Unlocked FPS: close this deferred draw object's bracket. */
+            mp6_fi_capture_model_end();
@@ -2507,0 +2626,10 @@
+    if (mp6ObjCensusActive()) { /* object census (matrix row: the applied wheel rotation) */
+        printf("[CENSUS-OBJ]   ObjDraw model=%d obj=%s faceCount=%d mtxR0=(%.3f,%.3f,%.3f,%.1f) mtxR1=(%.3f,%.3f,%.3f,%.1f) mtxR2=(%.3f,%.3f,%.3f,%.1f)\n",
+               (int)(drawObj->model - Hu3DData),
+               drawObj->object->name ? drawObj->object->name : "(null)",
+               drawObj->object->mesh.face ? drawObj->object->mesh.face->count : -1,
+               drawObj->matrix[0][0], drawObj->matrix[0][1], drawObj->matrix[0][2], drawObj->matrix[0][3],
+               drawObj->matrix[1][0], drawObj->matrix[1][1], drawObj->matrix[1][2], drawObj->matrix[1][3],
+               drawObj->matrix[2][0], drawObj->matrix[2][1], drawObj->matrix[2][2], drawObj->matrix[2][3]);
+        fflush(stdout);
+    }
@@ -2692,0 +2821,119 @@
+    }
+    if (mp6DlDumpEnv() && mp6DlDumpEnv()[0] == '*') { /* sweep mode: one line per built object */
+        HSF_FACE *fp = faceBuf->data;
+        HuVecF *vtx = objPtr->mesh.vertex ? (HuVecF *)objPtr->mesh.vertex->data : NULL;
+        s32 vtxN = objPtr->mesh.vertex ? objPtr->mesh.vertex->count : 0;
+        s32 stN = objPtr->mesh.st ? objPtr->mesh.st->count : 0;
+        s32 fi, k, badV = 0, badS = 0;
+        float mn = 1e30f, mx = -1e30f;
+        for (fi = 0; fi < vtxN; fi++) {
+            float ax = vtx[fi].x < 0 ? -vtx[fi].x : vtx[fi].x;
+            float ay = vtx[fi].y < 0 ? -vtx[fi].y : vtx[fi].y;
+            float az = vtx[fi].z < 0 ? -vtx[fi].z : vtx[fi].z;
+            float m = ax > ay ? ax : ay; if (az > m) m = az;
+            if (m < mn) mn = m; if (m > mx) mx = m;
+        }
+        for (fi = 0; fi < faceBuf->count; fi++) {
+            HSF_FACE *f = &fp[fi];
+            s32 nIdx = ((f->typeSrc & HSF_FACE_MASK) == HSF_FACE_QUAD) ? 4 :
+                       ((f->typeSrc & HSF_FACE_MASK) == HSF_FACE_TRI) ? 3 : 0;
+            for (k = 0; k < nIdx; k++) {
+                if (f->index[k].vertex < 0 || f->index[k].vertex >= vtxN) badV++;
+                if (stN > 0 && (f->index[k].st < -1 || f->index[k].st >= stN)) badS++;
+            }
+            if ((f->typeSrc & HSF_FACE_MASK) == HSF_FACE_TRISTRIP) {
+                HSF_FACE_INDEX *sd = f->strip.data;
+                u32 sc = f->strip.count, m2;
+                for (m2 = 0; m2 < 3; m2++) {
+                    if (f->strip.index[m2].vertex < 0 || f->strip.index[m2].vertex >= vtxN) badV++;
+                }
+                for (m2 = 0; sd && m2 < sc; m2++) {
+                    if (sd[m2].vertex < 0 || sd[m2].vertex >= vtxN) badV++;
+                    if (stN > 0 && (sd[m2].st < -1 || sd[m2].st >= stN)) badS++;
+                }
+            }
+        }
+        printf("[DL-SWEEP] obj=%s faces=%d vtxN=%d dl=%d/%d badV=%d badS=%d vmag=[%g..%g]%s\n",
+               objPtr->name ? objPtr->name : "(null)", faceBuf->count, vtxN,
+               (int)totalSize, (int)DLTotalNum, (int)badV, (int)badS, mn, mx,
+               ((int)totalSize > (int)DLTotalNum || badV || badS) ? "  <-- ANOMALY" : "");
+        fflush(stdout);
+    }
+    if (mp6DlDumpWant(objPtr->name)) { /* DL/vertex ground-truth dump -- see mp6DlDumpWant above */
+        char path[128];
+        FILE *df;
+        snprintf(path, sizeof(path), "build/dldump_%s.txt", objPtr->name);
+        df = fopen(path, "w");
+        if (df) {
+            HSF_FACE *fp = faceBuf->data;
+            HuVecF *vtx = objPtr->mesh.vertex ? (HuVecF *)objPtr->mesh.vertex->data : NULL;
+            s32 vtxN = objPtr->mesh.vertex ? objPtr->mesh.vertex->count : 0;
+            HuVec2f *stp = objPtr->mesh.st ? (HuVec2f *)objPtr->mesh.st->data : NULL;
+            s32 stN = objPtr->mesh.st ? objPtr->mesh.st->count : 0;
+            s32 nrmN = objPtr->mesh.normal ? objPtr->mesh.normal->count : 0;
+            s32 colN = objPtr->mesh.color ? objPtr->mesh.color->count : 0;
+            s32 fi, k, badV = 0, badS = 0;
+            float mn = 1e30f, mx = -1e30f;
+            const u8 *dl = (const u8 *)DLBufStartP;
+            fprintf(df, "obj=%s faces=%d vtxN=%d stN=%d nrmN=%d colN=%d dlSize=%d dlCap=%d batches=%d\n",
+                    objPtr->name, faceBuf->count, vtxN, stN, nrmN, colN, (int)totalSize, (int)DLTotalNum, (int)matChgCnt);
+            for (fi = 0; fi < vtxN; fi++) {
+                float ax = vtx[fi].x < 0 ? -vtx[fi].x : vtx[fi].x;
+                float ay = vtx[fi].y < 0 ? -vtx[fi].y : vtx[fi].y;
+                float az = vtx[fi].z < 0 ? -vtx[fi].z : vtx[fi].z;
+                float m = ax > ay ? ax : ay; if (az > m) m = az;
+                if (m < mn) mn = m; if (m > mx) mx = m;
+            }
+            fprintf(df, "vtx magnitude min=%g max=%g\n", mn, mx);
+            for (fi = 0; fi < vtxN && fi < 8; fi++) {
+                fprintf(df, "  vtx[%d]=(%g, %g, %g)\n", fi, vtx[fi].x, vtx[fi].y, vtx[fi].z);
+            }
+            for (fi = 0; fi < stN && fi < 8; fi++) {
+                fprintf(df, "  st[%d]=(%g, %g)\n", fi, stp[fi].x, stp[fi].y);
+            }
+            for (fi = 0; fi < faceBuf->count; fi++) {
+                HSF_FACE *f = &fp[fi];
+                s32 nIdx = ((f->typeSrc & HSF_FACE_MASK) == HSF_FACE_QUAD) ? 4 :
+                           ((f->typeSrc & HSF_FACE_MASK) == HSF_FACE_TRI) ? 3 : 0;
+                for (k = 0; k < nIdx; k++) {
+                    if (f->index[k].vertex < 0 || f->index[k].vertex >= vtxN) badV++;
+                    if (stN > 0 && (f->index[k].st < -1 || f->index[k].st >= stN)) badS++;
+                }
+                if (fi < 24) {
+                    fprintf(df, "face[%d] type=%04x mat=%d", fi, (unsigned)f->typeSrc, f->mat & 0xFFF);
+                    if ((f->typeSrc & HSF_FACE_MASK) == HSF_FACE_TRISTRIP) {
+                        u32 m2;
+                        for (m2 = 0; m2 < 3; m2++) {
+                            fprintf(df, " (v=%d n=%d c=%d st=%d)", f->strip.index[m2].vertex,
+                                    f->strip.index[m2].normal, f->strip.index[m2].color, f->strip.index[m2].st);
+                        }
+                        fprintf(df, " STRIP count=%u:", (unsigned)f->strip.count);
+                        for (m2 = 0; f->strip.data && m2 < f->strip.count && m2 < 8; m2++) {
+                            fprintf(df, " (v=%d n=%d c=%d st=%d)", f->strip.data[m2].vertex,
+                                    f->strip.data[m2].normal, f->strip.data[m2].color, f->strip.data[m2].st);
+                        }
+                    } else {
+                        for (k = 0; k < (nIdx ? nIdx : 3); k++) {
+                            fprintf(df, " (v=%d n=%d c=%d st=%d)", f->index[k].vertex,
+                                    f->index[k].normal, f->index[k].color, f->index[k].st);
+                        }
+                    }
+                    {
+                        HSF_MATERIAL *fm = &objPtr->mesh.material[f->mat & 0xFFF];
+                        fprintf(df, " | mat vtxMode=%d attrNum=%d flags=%08x pass=%d\n",
+                                fm->vtxMode, (int)fm->attrNum, (unsigned)fm->flags, fm->pass);
+                    }
+                }
+            }
+            fprintf(df, "out-of-range indices: vertex=%d st=%d (over %d faces)\n", (int)badV, (int)badS, faceBuf->count);
+            for (fi = 0; fi < matChgCnt && fi < 8; fi++) {
+                fprintf(df, "batch[%d] dlOfs=%u flags=%x\n", fi, (unsigned)DrawData[fi].dlOfs, (unsigned)DrawData[fi].flags);
+            }
+            for (fi = 0; fi < (s32)totalSize && fi < 1024; fi++) {
+                fprintf(df, "%02x%s", dl[fi], ((fi & 15) == 15) ? "\n" : " ");
+            }
+            fprintf(df, "\n");
+            fclose(df);
+            printf("[DL-DUMP] wrote %s (faces=%d dlSize=%d)\n", path, faceBuf->count, (int)totalSize);
+            fflush(stdout);
+        }
@@ -2730 +2976,8 @@ static void MDFaceDraw(HSF_OBJECT *objPtr, HSF_FACE *face)
-        DrawData[drawCnt].dlOfs = (u32) DLBufP - (u32) DLBufStartP;
+        {
+            uintptr_t current = (uintptr_t)DLBufP;
+            uintptr_t start = (uintptr_t)DLBufStartP;
+            if(current < start || current - start > INT32_MAX) {
+                OSReport("[FATAL] HSF display-list offset outside s32 range\n");
+                _Exit(EXIT_FAILURE);
+            }
+            DrawData[drawCnt].dlOfs = (s32)(current - start);
+        }
@@ -2834,0 +3082,39 @@
+                /* Real, unpatched original decomp gap, same class as
+                 * game/hsfman.c's Hu3DZClear and game/sprput.c's
+                 * HuSpr3DDisp: this GXBegin(GX_TRIANGLES, ...,
+                 * faceNumBuf[drawCnt]) is always followed by a loop
+                 * emitting EXACTLY faceNumBuf[drawCnt] vertices, so on real
+                 * hardware (GX FIFO ends a primitive by vertex count, no
+                 * explicit End needed) this was always complete and correct
+                 * with no GXEnd() call. Unlike HuSpr3DDisp, this one is NOT
+                 * safe to leave to the bridge's general auto-close
+                 * tolerance: this whole per-material batch is recorded
+                 * inside a GXBeginDisplayList/GXEndDisplayList bracket (see
+                 * 3 lines up / below), and Aurora's own fifo module tracks
+                 * the display-list recording buffer completely separately
+                 * from the immediate-mode buffer that GXBegin/GXEnd's
+                 * byte-count validation always reads from, even while
+                 * GXBeginDisplayList has redirected every write to the
+                 * OTHER buffer -- so by the time the bridge's tolerance
+                 * finally calls the real GXEnd() (the next GXBegin,
+                 * typically well after this material's own
+                 * GXEndDisplayList already returned), whatever unrelated
+                 * IMMEDIATE-mode GX traffic happened to run in between gets
+                 * misattributed as extra bytes for THIS primitive. Caught
+                 * directly (Aurora build, fileseldll reached for the first
+                 * time): "GXEnd: vertex count mismatch: GXBegin declared 60
+                 * vertices (6B each, 360B total) but 389 bytes were written
+                 * (64 vertices)". A real, explicit GXEnd() here -- BEFORE
+                 * GXEndDisplayList() falls through below -- is a pure
+                 * no-op on real hardware (zero behavior change: it already
+                 * correctly inferred completion from the vertex count)
+                 * while guaranteeing Aurora's own (buffer-blind) byte
+                 * measurement always sees a 0-byte delta at this exact
+                 * point, which trivially satisfies its validation gate
+                 * instead of leaving the primitive open for the bridge's
+                 * necessarily-coarser, wrong-buffer fallback to eventually
+                 * mis-close. */
+                GXEnd();
@@ -2904,0 +3191,6 @@
+                /* Same class and same fix as the HSF_FACE_TRI case above
+                 * (this function's own comment there has the full
+                 * root-cause story) -- GXQUADS' own vertex loop always
+                 * emits exactly faceNumBuf[drawCnt] vertices too. */
+                GXEnd();
@@ -2975,0 +3268,6 @@
+                /* Same class and same fix as the HSF_FACE_TRI case above
+                 * (this function's own comment there has the full
+                 * root-cause story) -- the initial 3 vertices plus this
+                 * strip loop always emit exactly faceNumBuf[drawCnt]
+                 * vertices in total too. */
+                GXEnd();
@@ -2993 +3292 @@
-static s32 MakeCalcNBT(HSF_OBJECT *objPtr, HSF_FACE *face, s16 endVtx, s16 startVtx) {
+static void MakeCalcNBT(HSF_OBJECT *objPtr, HSF_FACE *face, s16 endVtx, s16 startVtx) {
@@ -3028 +3327 @@
-static s32 MakeNBT(HSF_OBJECT *objPtr, HSF_FACE *face, s16 endVtx, s16 startVtx) {
+static void MakeNBT(HSF_OBJECT *objPtr, HSF_FACE *face, s16 endVtx, s16 startVtx) {
@@ -3059 +3358 @@ static void MDFaceCnt(HSF_OBJECT *objPtr, HSF_FACE *face) {
-    if(matP != materialBak || (polyTypeBak != face->type & 7) || (face->type & 7) == 4) {
+    if(matP != materialBak || polyTypeBak != (face->type & 7) || (face->type & 7) == 4) {
@@ -3438,0 +3738,24 @@
+static HSF_OBJECT *mp6_hsf_duplicate_remap(const HSF_DATA *hsf,
+                                            HSF_OBJECT *objectCopy,
+                                            const HSF_OBJECT *source)
+{
+    uintptr_t base;
+    uintptr_t address;
+    uintptr_t delta;
+    size_t bytes;
+    if(source == NULL) return NULL;
+    if(hsf == NULL || objectCopy == NULL || hsf->object == NULL
+            || hsf->objectNum <= 0
+            || (size_t)hsf->objectNum > SIZE_MAX / sizeof(HSF_OBJECT)) {
+        OSReport("[FATAL] invalid HSF duplicate object array\n");
+        _Exit(EXIT_FAILURE);
+    }
+    base = (uintptr_t)hsf->object;
+    address = (uintptr_t)source;
+    bytes = (size_t)hsf->objectNum * sizeof(HSF_OBJECT);
+    if(address < base || (delta = address - base) >= bytes
+            || delta % sizeof(HSF_OBJECT) != 0) {
+        OSReport("[FATAL] HSF duplicate pointer outside source object array\n");
+        _Exit(EXIT_FAILURE);
+    }
+    return (HSF_OBJECT *)((u8 *)objectCopy + delta);
+}
+
@@ -3447,2 +3771,10 @@ HSF_OBJECT *Hu3DObjDuplicate(HSF_DATA *hsf, u32 mallocNo)
-    objPtr = HuMemDirectMallocNum(HEAP_MODEL, hsf->objectNum * sizeof(HSF_OBJECT), mallocNo);
+    if(hsf == NULL || hsf->object == NULL || hsf->objectNum <= 0
+            || (size_t)hsf->objectNum > (size_t)INT32_MAX / sizeof(HSF_OBJECT)) {
+        OSReport("[FATAL] invalid HSF object duplicate request\n");
+        _Exit(EXIT_FAILURE);
+    }
+    objPtr = HuMemDirectMallocNum(HEAP_MODEL,
+        hsf->objectNum * sizeof(HSF_OBJECT), mallocNo);
+    if(objPtr == NULL) {
+        OSReport("[FATAL] HSF object duplicate allocation failed\n");
+        _Exit(EXIT_FAILURE);
+    }
     objBuf = objPtr;
@@ -3455 +3787 @@ HSF_OBJECT *Hu3DObjDuplicate(HSF_DATA *hsf, u32 mallocNo)
-                objReplicaP->mesh.replica = (HSF_OBJECT*) ((u8 *) objBuf + ((u32)objReplicaP->mesh.replica - (u32)hsf->object));
+                objReplicaP->mesh.replica = mp6_hsf_duplicate_remap(hsf, objBuf, srcObj->mesh.replica);
@@ -3458 +3790 @@ HSF_OBJECT *Hu3DObjDuplicate(HSF_DATA *hsf, u32 mallocNo)
-                objPtr->mesh.parent = (HSF_OBJECT*) ((u8 *)objBuf + ((u32)srcObj->mesh.parent - (u32)hsf->object));
+                objPtr->mesh.parent = mp6_hsf_duplicate_remap(hsf, objBuf, srcObj->mesh.parent);
@@ -3460 +3792,9 @@ HSF_OBJECT *Hu3DObjDuplicate(HSF_DATA *hsf, u32 mallocNo)
-            objPtr->mesh.child = HuMemDirectMallocNum(HEAP_MODEL, srcObj->mesh.childNum*sizeof(HSF_OBJECT *), mallocNo);
+            if(srcObj->mesh.childNum != 0) {
+                objPtr->mesh.child = HuMemDirectMallocNum(HEAP_MODEL,
+                    srcObj->mesh.childNum*sizeof(HSF_OBJECT *), mallocNo);
+                if(objPtr->mesh.child == NULL) {
+                    OSReport("[FATAL] HSF duplicate child allocation failed\n");
+                    _Exit(EXIT_FAILURE);
+                }
+            } else {
+                objPtr->mesh.child = NULL;
+            }
@@ -3462,0 +3803,4 @@ HSF_OBJECT *Hu3DObjDuplicate(HSF_DATA *hsf, u32 mallocNo)
+                if(objPtr->constData == NULL) {
+                    OSReport("[FATAL] HSF duplicate const-data allocation failed\n");
+                    _Exit(EXIT_FAILURE);
+                }
@@ -3466 +3810 @@ HSF_OBJECT *Hu3DObjDuplicate(HSF_DATA *hsf, u32 mallocNo)
-                objPtr->mesh.child[j] = (HSF_OBJECT*) ((u8 *)objBuf + ((u32)srcObj->mesh.child[j] - (u32)hsf->object));
+                objPtr->mesh.child[j] = mp6_hsf_duplicate_remap(hsf, objBuf, srcObj->mesh.child[j]);
@@ -3495 +3793,23 @@
-            objPtr->mesh.attribute = (HSF_ATTRIBUTE *) (attrBuf + (objPtr->mesh.attribute - hsf->attribute));
+            /* 64-bit/checked-build portability fix, NULL-in-pointer-rebase
+             * class: a mesh with no attributes has mesh.attribute == NULL
+             * -- BY DESIGN, on real hardware too (ObjectLoad's own -1 "no
+             * attributes" sentinel, the classic bit-31 idiom). Hardware's
+             * version of this line then computes `attrBuf + (NULL - base)`
+             * for such meshes: a garbage pointer, written back, and
+             * provably never read (GC mode select runs fine -- the
+             * attribute pointer of an attribute-less mesh has no
+             * consumer). This port's checked build (zig cc UBSan) instead
+             * panics at the arithmetic itself ("addition of unsigned
+             * offset ... overflowed", reproduced via mdseldll fn_1_5614's
+             * Hu3DModelLink of the 30 linked fish models -- the first
+             * Hu3DModelLink call any flow in this port has ever reached).
+             * Keeping NULL for the linked copy is observably identical on
+             * the never-read contract, and strictly better diagnostics if
+             * some future path DOES read it: an immediate, symbolized
+             * null-deref at the read site instead of a wild pointer. */
+            if (objPtr->mesh.attribute != NULL) {
+                objPtr->mesh.attribute = (HSF_ATTRIBUTE *) (attrBuf + (objPtr->mesh.attribute - hsf->attribute));
+            }
