--- a/src/game/data.c
+++ b/src/game/data.c
@@ -116 +116 @@
-    if(DataDirMax <= dirId) {
+    if(dirId < 0 || DataDirMax <= dirId) {
@@ -123,5 +123,8 @@
-        if(dirAMemP = HuARDirCheck(dataNum)) {
-            HuAR_ARAMtoMRAM(dirAMemP);
+        if((dirAMemP = HuARDirCheck(dataNum)) != 0) {
+            if(HuAR_ARAMtoMRAM(dirAMemP) == NULL) {
+                return NULL;
+            }
             while(HuARDMACheck());
             statId = HuDataReadChk(dataNum);
+            if(statId < 0) {
+                return NULL;
+            }
             readStat = &ReadDataStat[statId];
@@ -153 +156 @@
-    if(DataDirMax <= dirId) {
+    if(dirId < 0 || DataDirMax <= dirId) {
@@ -266 +269 @@
-    if(DataDirMax <= dirId) {
+    if(dirId < 0 || DataDirMax <= dirId) {
@@ -272,4 +275,6 @@
-        if(dirAMemP = HuARDirCheck(dataNum)) {
+        if((dirAMemP = HuARDirCheck(dataNum)) != 0) {
             OSReport("ARAM data num %x\n", dataNum);
-            HuAR_ARAMtoMRAM(dirAMemP);
+            if(HuAR_ARAMtoMRAM(dirAMemP) == NULL) {
+                return HU_DATA_STAT_NONE;
+            }
             statId = STAT_ID_ARAM;
@@ -299 +304 @@
-    if(DataDirMax <= dirId) {
+    if(dirId < 0 || DataDirMax <= dirId) {
@@ -471 +476 @@
-        if(DataDirMax <= dirId) {
+        if(dirId < 0 || DataDirMax <= dirId) {
@@ -625 +630 @@
-	if(dir >= (s32)DataDirMax) {
+	if(dir < 0 || dir >= (s32)DataDirMax) {
@@ -630,2 +635,2 @@
-		char panic_str[48];
-		sprintf(panic_str, "HuDataDVDdirDirectOpen: File Open Error(%08x)", dataNum);
+		char panic_str[64];
+		snprintf(panic_str, sizeof(panic_str), "HuDataDVDdirDirectOpen: File Open Error(%08x)", dataNum);
@@ -659 +664 @@
-	if((u32)buf & 0x3) {
+	if((uintptr_t)buf & 0x3u) {
@@ -3,0 +4 @@
+#include "be.h"
@@ -330,0 +332,3 @@
+    /* dirP's own directory/index table and the per-file rawLen/decodeType
+     * header are both big-endian-packed -- read via be32() rather than a
+     * native u32 dereference. */
@@ -333 +337 @@
-    readStat->fileDataP = PTR_OFFSET(readStat->dirP, *ptr);
+    readStat->fileDataP = PTR_OFFSET(readStat->dirP, be32(ptr));
@@ -335,2 +339,2 @@
-    readStat->rawLen = *ptr++;
-    readStat->decodeType = *ptr++;
+    readStat->rawLen = be32(ptr); ptr++;
+    readStat->decodeType = be32(ptr); ptr++;
@@ -670,0 +675,2 @@
+		/* Aligned case: same big-endian header as the unaligned branch above,
+		 * just read a word at a time -- needs the same be32() fix. */
@@ -672,2 +678,2 @@
-		rawLen = *data++;
-		decodeType = *data++;
+		rawLen = (s32)be32(data); data++;
+		decodeType = (s32)be32(data); data++;
