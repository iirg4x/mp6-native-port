--- a/src/REL/bootDll/opening.c
+++ b/src/REL/bootDll/opening.c
@@ -14,0 +15 @@
+#include "mp6_widescreen.h" /* widescreen support */
@@ -161,0 +163 @@ void OpeningCreate(void)
+    mp6_widescreen_extrude_model(OpeningMdlId[0]);
@@ -536,2 +538,4 @@ static void PostDrawHook(s16 layerNo)
-    Hu3DCameraPerspectiveSet(OPENING_CAMERA_BIT, 30.f, 20.f, 15000.f, 1.2f);
-    Hu3DCameraViewportSet(OPENING_CAMERA_BIT, 0.0f, 0.0f, 640.f, 480.f, 0.0f, 1.0f);
+    Hu3DCameraPerspectiveSet(OPENING_CAMERA_BIT, 30.f, 20.f, 15000.f,
+                             1.2f * mp6_widescreen_scale_factor());
+    Hu3DCameraViewportSet(OPENING_CAMERA_BIT, 0.0f, 0.0f,
+                          mp6_widescreen_render_width(), 480.f, 0.0f, 1.0f);
