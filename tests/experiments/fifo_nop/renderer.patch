diff --git a/lib/gx/command_processor.cpp b/lib/gx/command_processor.cpp
--- a/lib/gx/command_processor.cpp
+++ b/lib/gx/command_processor.cpp
@@ -425,6 +425,8 @@
   g_gxState.stateDirty = true; // Never merge across old/new array snapshots.
 }
 
+#include "skip_nop_padding.inc"
+
 void process(const u8* data, u32 size, bool bigEndian) {
   ZoneScoped;
   u32 pos = 0;
@@ -436,6 +438,7 @@
 
     switch (opcode) {
     case CP_CMD_NOP:
+      pos = skip_nop_padding(data, pos, size);
       continue;
 
     case CP_CMD_LOAD_BP_REG: {
diff --git a/lib/gx/skip_nop_padding.inc b/lib/gx/skip_nop_padding.inc
new file mode 100644
--- /dev/null
+++ b/lib/gx/skip_nop_padding.inc
@@ -0,0 +1,13 @@
+// Called only after the dispatcher has consumed a NOP command. GX ignores
+// the low three opcode bits, so all bytes 0x00..0x07 are padding here. Keep
+// the first real command for the normal decoder, including all payloads.
+static u32 skip_nop_padding(const u8* data, u32 pos, u32 size) {
+  while (size - pos >= sizeof(uint64_t)) {
+    uint64_t word;
+    std::memcpy(&word, data + pos, sizeof(word));
+    if (word & UINT64_C(0xf8f8f8f8f8f8f8f8)) break;
+    pos += sizeof(word);
+  }
+  while (pos < size && (data[pos] & 0xf8) == 0) ++pos;
+  return pos;
+}
